// For var
var price1 = 500; //Declared price + Initialized price
var price1; // Redeclared price

console.log(price1);


// Let
let price2 = 400; //Declared price + Initialized price
//let price2; // If we  use let we cannot redeclare 
price2 = 1000; //but we can reinitialize

console.log(price2);